import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Configuración para archivos estáticos (documentos subidos)
  serverExternalPackages: ["bcryptjs"],
};

export default nextConfig;
